import React from 'react';
import PasswordForm from '../containers/Form/Password';
import { Header } from '../components/Header/User';

const Password = () => (
  <div>
    <Header>Change Password</Header>
    <PasswordForm />
  </div>
)

export default Password;
